[theme]
# Using Streamlit's built-in light theme as default
# Users can switch to dark mode via Settings menu (☰ → Settings → Theme)
base = "light"
primaryColor = "#4CAF50"
font = "sans serif"

[server]
headless = true
enableCORS = false
enableXsrfProtection = true
