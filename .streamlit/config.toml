[theme]
# Using Streamlit's built-in dark theme as default
# Users can switch to light mode via Settings menu (☰ → Settings → Theme)
base = "dark"
primaryColor = "#4CAF50"
font = "sans serif"

[server]
headless = true
enableCORS = false
enableXsrfProtection = true
